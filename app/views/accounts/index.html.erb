<h1>posts for all users</h1>
   <div class="container">
      <div class="row">
       <div class="col-8">
   <% @posts.each do |post|%>
        <%=post.account.user_name%>
       <%=image_tag url_for(post.image), class: "img img-fluid"%>
   <%end%>
   </div>
  </div>
</div>
   